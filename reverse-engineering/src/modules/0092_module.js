/**
 * Webpack Module #92
 * Type: unknown
 */

function (e, t, n) {
    "use strict";
    var o = n(194),
      i = TypeError;
    e.exports = function (e) {
      if (o(e)) throw new i("Can't call method on " + e);
      return e;
    };
  }